% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/0.annotation.R
\name{leo_map_gene_to_chrbp}
\alias{leo_map_gene_to_chrbp}
\title{Map Gene Symbols to Genomic Positions}
\usage{
leo_map_gene_to_chrbp(
  genes,
  gene_col = NULL,
  method = c("bioconductor", "gtf"),
  genome = c("hg19", "hg38"),
  ...
)
}
\arguments{
\item{genes}{A character vector of gene symbols or a data frame containing gene symbols.}

\item{gene_col}{The column name of gene symbols if \code{genes} is a data frame.}

\item{method}{The method to use: \code{"bioconductor"} or \code{"gtf"}.}

\item{genome}{The genome assembly to use: \code{"hg19"} or \code{"hg38"}.}

\item{...}{Additional arguments to pass to the GTF method.}
}
\value{
A data frame with columns: gene_symbol, chr, bp_start, bp_end, strand.
}
\description{
This function maps gene symbols to their genomic positions (chromosome,
start, end, strand) using the specified method and genome assembly.
}
\details{
The function supports two methods:
\itemize{
\item \code{"bioconductor"}: uses Bioconductor packages. See \code{\link[=map_using_bioconductor]{map_using_bioconductor()}}.
\item \code{"gtf"}: uses a GTF file. See \code{\link[=map_using_gtf]{map_using_gtf()}}.
}
}
\examples{
\dontrun{
# Using Bioconductor method with a character vector of gene symbols
leo_map_gene_to_chrbp(genes = c("TP53", "BRCA1", "EGFR"), method = "bioconductor", genome = "hg19")

# Using Bioconductor method with a data frame
leo_map_gene_to_chrbp(genes = data.frame(gene_name = c("TP53", "BRCA1", "EGFR"), value = c(1.2, 3.4, 5.6)),
                      gene_col = "gene_name", method = "bioconductor", genome = "hg19")

# Using GTF method with a character vector of gene symbols
leo_map_gene_to_chrbp(genes = c("TP53", "BRCA1", "EGFR"), method = "gtf", genome = "hg38")

# Using GTF method with a data frame
leo_map_gene_to_chrbp(genes = data.frame(gene_name = c("TP53", "BRCA1", "EGFR"), value = c(1.2, 3.4, 5.6)),
                      gene_col = "gene_name", method = "gtf", genome = "hg38", download_dir = "~/project/ref/gtf")
}
}
