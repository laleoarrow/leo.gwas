% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/csMR_tools.R
\name{csMR_step3_run}
\alias{csMR_step3_run}
\title{Run csMR Step 3}
\usage{
csMR_step3_run(
  repo_dir = "~/Project/software/csMR",
  config_file = "./output/csMR/step2_config/config.yml",
  jobs = NULL,
  forcerun = NULL,
  work_flow.snakefile = NULL,
  env_name = "csMR",
  conda = Sys.which("conda"),
  dry_run = FALSE,
  log_file = NULL
)
}
\arguments{
\item{repo_dir}{csMR repository directory.}

\item{config_file}{Config file path.}

\item{jobs}{Number of Snakemake jobs. If \code{NULL}, run with bare \code{-j} and let
Snakemake use all available cores.}

\item{forcerun}{Optional Snakemake targets/rules to force-run. Default
\code{NULL} means no \code{--forcerun} is added.}

\item{work_flow.snakefile}{Snakemake workflow file path. If \code{NULL}, use
\code{file.path(repo_dir, "work_flow.snakefile")}.}

\item{env_name}{Conda env name.}

\item{conda}{Path to conda.}

\item{dry_run}{Whether to run \code{--dry-run}.}

\item{log_file}{Optional log file path.}
}
\value{
A list with \code{command} (copyable shell command), \code{status}, \code{log_file},
\code{r_lib}, and \code{r_home}. Output streams to console in real time and is also
saved to \code{log_file}.
}
\description{
Run the csMR Snakemake workflow with \code{R_HOME} and \code{R_LIBS} explicitly set
from the target csMR conda environment.
}
\details{
The first four arguments (\code{repo_dir}, \code{config_file}, \code{jobs}, \code{forcerun})
are the most commonly adjusted by users in routine runs.
}
\examples{
\dontrun{
csMR_step3_run(repo_dir = "~/Project/software/csMR", jobs = NULL, dry_run = TRUE)
}
}
