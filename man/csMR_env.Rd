% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/csMR_tool.R
\name{csMR_env}
\alias{csMR_env}
\title{Configure Conda Environment for csMR}
\usage{
csMR_env(
  repo_dir = file.path(path.expand("~"), "tools", "csMR"),
  env_name = "csMR",
  conda = Sys.which("conda"),
  repo_url = "https://github.com/rhhao/csMR.git",
  ref = "main",
  use_mamba = TRUE,
  overwrite = FALSE,
  update_repo = TRUE,
  install_plink = TRUE,
  install_r_pkgs = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{repo_dir}{Directory where the csMR repository is stored.}

\item{env_name}{Name of the conda environment.}

\item{conda}{Path to the conda executable. Defaults to \code{Sys.which("conda")}.}

\item{repo_url}{GitHub URL of the csMR repository.}

\item{ref}{Git branch/tag to checkout for csMR source code.}

\item{use_mamba}{Whether to prefer \code{mamba} for environment solving.}

\item{overwrite}{Whether to remove an existing environment before recreating.}

\item{update_repo}{Whether to \verb{git pull --ff-only} when repo already exists.}

\item{install_plink}{Whether to install PLINK 1.9 in the conda environment.}

\item{install_r_pkgs}{Whether to install and verify required csMR R packages
in-env. If \code{FALSE}, skip R package installation/checks.}

\item{verbose}{Whether to print setup logs.}
}
\value{
A list containing \code{repo_dir}, \code{env_name}, \code{env_prefix}, \code{env_file},
\code{r_library}, and two convenience shell snippets (\code{export_r_libs},
\code{run_snakemake_example}).
}
\description{
\code{csMR_env()} automates environment setup for the official csMR pipeline:
it clones/updates the csMR repository, creates (or updates) a conda
environment from \code{envs/envpy3.yml}, installs missing R dependencies used by
csMR scripts, and runs basic command/package checks.
}
\details{
Create and validate a reproducible runtime environment for the
\href{https://github.com/rhhao/csMR}{csMR} workflow.

The method follows the csMR paper workflow (Nat Commun 2024, 15:4890):
(1) locus-level precomputation from GWAS and single-cell eQTL summary data,
(2) SuSiE-based fine-mapping and colocalization, and (3) cell-type-specific
MR with sensitivity analyses. This function only prepares software/runtime
dependencies; it does not execute the full csMR analysis.
}
\examples{
\dontrun{
# First-time setup
cfg <- csMR_env(repo_dir = "~/tools/csMR", env_name = "csMR")
cfg$run_snakemake_example

# Fast health-check for an existing environment
csMR_env(repo_dir = "~/tools/csMR", env_name = "csMR", update_repo = FALSE)

# Quick solver smoke-test (skip heavy R package installation)
csMR_env(
  repo_dir = "~/tools/csMR",
  env_name = "csMR_smoke",
  install_r_pkgs = FALSE
)
}

}
