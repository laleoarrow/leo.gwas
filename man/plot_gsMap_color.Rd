% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/0.visual_tools.R
\name{plot_gsMap_color}
\alias{plot_gsMap_color}
\title{Build color maps for gsMap plots (helper function)}
\usage{
plot_gsMap_color(
  annos,
  anno_colors = NULL,
  color = NULL,
  highlight_tissue = NULL,
  highlight_color = NULL
)
}
\arguments{
\item{annos}{Character vector of annotation levels (unique, ordered).}

\item{anno_colors}{Named colors for some/all levels (preferred over \code{color}).}

\item{color}{Named colors as fallback if \code{anno_colors} is NULL.}

\item{highlight_tissue}{Character vector of levels to highlight; fallback to first of \code{annos} if none valid.}

\item{highlight_color}{Single color string or a named vector mapping highlight levels to colors.}
}
\value{
A list with:
\item{final_map}{named colors for all \code{annos} after auto-fill & highlight override (for full plot)}
\item{hi_levels}{chosen highlight levels}
\item{hi_map}{named colors for highlight levels only (for highlight-only plot)}
}
\description{
gsMap_plot Helper to build color mappings (partial fill, highlight override).
}
\examples{
library(patchwork)
library(ggplot2)
# Simulate a small dataframe
df <- data.frame(sx = rnorm(30), sy = rnorm(30),
                 annotation = rep(c("TissueA","TissueB","TissueC"), each = 10),
                 logp = runif(30, 1, 10))

# User provides partial colors; others will be auto-filled
cm <- plot_gsMap_color(annos = unique(df$annotation),
                       anno_colors = c(TissueA = "#F2B701"),
                       highlight_tissue = "TissueB",
                       highlight_color  = "#FF0000")

# Full plot (manual scale with final_map)
gg <- ggplot2::ggplot(df, ggplot2::aes(sx, sy, color = annotation)) +
  ggplot2::geom_point() +
  ggplot2::scale_color_manual(values = cm$final_map)

# Highlight-only plot (only highlight levels mapped)
gg_hi <- ggplot2::ggplot(df, ggplot2::aes(sx, sy, color = annotation)) +
  ggplot2::geom_point() +
  ggplot2::scale_color_manual(values = cm$hi_map, name = "Highlight")
gg | gg_hi
}
