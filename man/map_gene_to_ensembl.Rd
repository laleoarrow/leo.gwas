% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/0.annotation.R
\name{map_gene_to_ensembl}
\alias{map_gene_to_ensembl}
\title{Map Gene Symbols to Ensembl IDs}
\usage{
map_gene_to_ensembl(
  genes,
  gene_col = NULL,
  method = c("org.Hs.eg.db", "biomart"),
  genome = c("hg19", "hg38"),
  type = c("first", "combine"),
  sep = "/",
  batch_size = 100
)
}
\arguments{
\item{genes}{Input containing gene symbols. Can be:
\itemize{
\item Character vector of gene symbols
\item Data frame containing gene symbol column
}}

\item{gene_col}{Column name containing gene symbols when \code{genes} is data frame}

\item{method}{Mapping methodology:
\itemize{
\item "org.Hs.eg.db": Local Bioconductor annotations (default)
\item "biomart": Ensembl BioMart service
}}

\item{genome}{Genome assembly version (BioMart only):
\itemize{
\item "hg19": GRCh37 (default)
\item "hg38": GRCh38
}}

\item{type}{Multi-mapping handling:
\itemize{
\item "first": Return first valid ID (default)
\item "combine": Concatenate multiple IDs
}}

\item{sep}{Separator for combined IDs (default: "/")}

\item{batch_size}{BioMart query batch size (default: 100)}
}
\value{
Data frame with original data + ensembl_id column
}
\description{
This function provides robust gene symbol to Ensembl ID mapping through:
\enumerate{
\item{Local \code{org.Hs.eg.db} annotations (default)}
\item{Ensembl BioMart web service (requires internet)}
}
}
\examples{
\dontrun{
# Local annotation method
genes <- c("TP53", "BRCA1", "VEGFA")
result_local <- map_gene_to_ensembl(genes)

# BioMart with custom parameters
gene_df <- data.frame(
  my_symbol = c("TP53", "BRCA1", "NONEXISTENT"),
  values = rnorm(3)
)
result_biomart <- map_gene_to_ensembl(
  gene_df,
  gene_col = "my_symbol",
  method = "biomart",
  genome = "hg19",
  batch_size = 50
)
}
}
